<UserControl x:Class="CL8.UI.Views.Pages.Chats.CreateChatPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CL8.UI.Views.Pages.Chats"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:Background="Azure">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition />
            <RowDefinition Height="20" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Create your chat" FontSize="17" FontWeight="ExtraBlack" />
        </StackPanel>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Width="360">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Chat name:" TextAlignment="Right" Margin="5" VerticalAlignment="Center" Width="150" />
                    <TextBox Text="{Binding ChatName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource StdTBox}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="1">
                    <TextBlock Text="Chat description:" TextAlignment="Right" Margin="5" VerticalAlignment="Center" Width="150" />
                    <TextBox Text="{Binding ChatDescription, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource StdTBox}" />
                </StackPanel>
                <StackPanel Grid.Row="2" Margin="5">
                    <Button Content="Create" Style="{StaticResource gBtn}" Command="{Binding CreateChatCmd}" />
                </StackPanel>
            </Grid>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2">
            <TextBlock Text="{Binding Error}" Foreground="Red" />
        </StackPanel>
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Cancel" Style="{StaticResource gBtn}" Command="{Binding CancelCmd}" />
        </StackPanel>
    </Grid>
</UserControl>
